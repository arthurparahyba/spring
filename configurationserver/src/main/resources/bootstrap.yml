server:
  port: 8888
spring:
  cloud:
    config:
      server:
          encrypt:
            enabled: false
          git:
            uri: https://github.com/arthurparahyba/config-repo/
            search-paths: licensingservice, organizationservice
