server:
  port: 8888
spring:
  cloud:
    config:
      server:
          encrypt:
            enabled: false
          git:
            uri: https://github.com/arthurparahyba/spring/
            search-paths: config-repo/licensingservice, config-repo/organizationservice
